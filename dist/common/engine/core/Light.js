export class Light {
    constructor({ ambient = 0, shinines = 200, color = [255, 255, 255], // white
    intensity = 1, } = {}) {
        this.ambient = ambient;
        this.shinines = shinines;
        this.color = color;
        this.intensity = intensity;
    }
}
//# sourceMappingURL=Light.js.map